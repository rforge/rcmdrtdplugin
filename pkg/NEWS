Changes to Version 1.2-0

  o tkrplot package suggested rather than required to reflect changes on CRAN; if tkrplot is absent, several demos are disabled.
  
  o rgl is also now suggested rather than required, with similar consequences. 

Changes to Version 1.1-0

  o  General updating to conform to CRAN and newer Rcmdr conventions.

For changes to earlier version see the package CHANGES file.
